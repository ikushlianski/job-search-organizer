// all values from URL are initially strings
// should parse them to numbers as per need
export interface IterationId {
  iterationId: string;
}

export interface OpportunityId {
  opportunityId: string;
}
